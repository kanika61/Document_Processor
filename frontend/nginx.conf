server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: unknown paths (e.g. /documents/DOC-...) fall back to index.html
    location / {
        try_files $uri /index.html;
    }
}
